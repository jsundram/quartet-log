last_deploy/
.DS_Store
*.zip
wrapped/
.playwright-mcp/
md/*.html
!md/_pandoc_template.html
# Nothing in archive/ is tracked, and everything that lands there carries
# real names: the sheet snapshots, and the location cleanup's proposal and
# generated Apps Script (issue #39).
archive/
alias-output.txt
.dev-data-url
node_modules/
# Real player-name tables (personal data). The glob also catches editor/
# Dropbox duplicates like "aliases (1).js" so they can't get committed.
src/aliases*.js
!src/aliases.stub.js
test-results/
playwright-report/
